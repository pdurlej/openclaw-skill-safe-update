interface:
  display_name: "OpenClaw Safe Update"
  short_description: "Rehearse OpenClaw updates without touching runtime"
  default_prompt: "Use $openclaw-safe-update to rehearse an OpenClaw update and produce fail-closed evidence."
policy:
  allow_implicit_invocation: true
