interface:
  display_name: "OpenClaw Safe Upgrade Rehearsal"
  short_description: "Dry-run an OpenClaw upgrade and verify its evidence"
  default_prompt: "Use $openclaw-safe-update to dry-run an OpenClaw update, verify compatibility evidence, and stop before apply."
policy:
  allow_implicit_invocation: true
