# Changelog

## 1.3.0 - 2026-07-19

- Add a public-safe local inventory command and coverage-profile draft.
- Require an installation coverage profile for the default rehearsal path.
- Compare Node engine, dependency, executable, and lifecycle-script metadata.
- Block on incompatible or unproven Node requirements and changed lifecycle
  scripts.
- Generate an explicit post-upgrade E2E plan and rollback-aware operator plan.
- Expand the reusable GitHub workflow and upgrade experience intake.
- Bind the complete resolved installation, environment, add-ons, sidecars,
  configuration, and personalization into one closed candidate identity.
- Attest local installation completeness separately from declaration freshness.
- Add conservative gates for unknown inputs, state migration, rollback,
  environment, protocol, and contract risk.
- Add deterministic analysis caching and bounded parallel archive inspection.
- Keep impact mapping and optional model review additive and non-authoritative.
- Define hash-bound phase handoffs and one operator-facing status view.
- Publish the frozen shadow benchmark and retain the full additive baseline;
  selective omission remains disabled.

## 1.0.0

- Verify exact npm artifacts and registry integrity.
- Compare bounded package file surfaces and declared customization anchors.
- Emit hash-bound read-only evidence with `blocked` or
  `ready_for_operator_plan`.
